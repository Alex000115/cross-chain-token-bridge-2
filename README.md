# cross-chain-token-bridge-2
A professional implementation of a cross-chain messaging bridge. This repository utilizes the LayerZero V2 protocol to enable seamless token transfers between different blockchain networks (e.g., Ethereum to Polygon) using an Omnichain Fungible Token (OFT) standard.
